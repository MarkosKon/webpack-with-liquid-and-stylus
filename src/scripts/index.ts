import { add } from "lodash-es";

import "../styles/style.styl";

console.log("It works!");

const number1 = 1;
const number2 = 2;
console.log(`${number1} + ${number2} equals ${add(number1, number2)}`);
